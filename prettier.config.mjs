/** @type {import('prettier').Config} */
const config = {
    singleQuote: true,
    trailingComma: 'all',
    printWidth: 100,
    semi: true,
    tabWidth: 2,
    endOfLine: 'lf',
};

export default config;
